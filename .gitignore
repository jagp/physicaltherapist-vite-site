# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Brainstorming visual companion (session state holds the local server key + logs)
docs/superpowers/mocks/state/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local git worktrees + scratch archives (never commit)
.claude/worktrees/
archive.zip

# Local secrets — API keys live only on this machine (never commit)
.env
**/.env
.env.*
!.env.example
